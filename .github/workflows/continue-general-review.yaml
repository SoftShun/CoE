name: Continue General Review

on:
  pull_request:
    types: [opened, ready_for_review]
  issue_comment:
    types: [created]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  general-code-review:
    name: Continue General Review
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Run Continue PR Review Action
        uses: continuedev/continue/actions/general-review@4c93e8c92a3b8df92e5b9af1b6209d6e2676f71c
        with:
          continue-api-key: ${{ secrets.CONTINUE_API_KEY }}
          continue-org: "continuedev"
          continue-config: "continuedev/review-bot"
